{
  "name": "vzip-site",
  "version": "1.0.0",
  "description": "Marketing site for VZiP DEVELOGENZ with Tailwind build",
  "scripts": {
    "build": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify",
    "watch": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.2"
  }
}